import { AddUnderlinePipe } from './add-underline.pipe';

describe('AddUnderlinePipe', () => {
  it('create an instance', () => {
    const pipe = new AddUnderlinePipe();
    expect(pipe).toBeTruthy();
  });
});
